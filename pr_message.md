🎯 작업 목적
- 파일 메타데이터(라인 수, 글자 수)를 추출하여 향후 LLM이 컨텍스트 길이를 더 정확하게 고려할 수 있도록 돕습니다. (#167, #168)
- 프론트엔드 모델 카탈로그를 서버와 동기화하고, RAG 인덱싱 과정 중에도 저장소 분석 결과를 바로 볼 수 있도록 UX를 개선합니다. (#178, #179, #181)

✨ 주요 변경 사항
- [x] 프론트엔드 분석 뷰: RAG 인덱싱 대기 상태와 리포트 화면 렌더링 분리 (#178)
- [x] 프론트엔드 분석 뷰: window.alert, window.confirm 대신 커스텀 React 컴포넌트 모달(showConfirm) 구현 (#179)
- [x] 프론트엔드 폼: 모델 선택지를 gpt-4o-mini, gpt-4o 두 가지만 남겨 백엔드 카탈로그와 동기화 (#181)
- [x] 백엔드 파싱: ParsedFile 모델에 `chars` 필드 추가하고 `directory.py`에서 파싱 중 라인 수 및 글자 수를 추출하도록 변경 (#168, #167)
- [x] 백엔드 임베딩: DB 저장(`repository.py`) 시 `file_metadata`에 추출된 메타데이터 반영

🛠️ 리뷰 및 로컬 테스트 방법
- `npm run dev` 실행 후 분석 뷰(`analyze`) 진입 시 모달 및 인덱싱 프로세스가 매끄럽게 동작하는지 확인
- 대용량 리포지토리를 불러왔을 때 경고 창이 커스텀 UI로 잘 뜨는지 확인

📸 실행 결과 (선택)
(없음)

⚠️ 리뷰어에게 당부하는 점
@woovii000 @KimHyo1 @smmini
- `CodeNode` 테이블의 JSONB 필드 `file_metadata`에 스키마 변경 사항이 안전하게 반영되었으므로 백엔드 DB 동작 이상 유무를 점검해 주시면 감사합니다!
